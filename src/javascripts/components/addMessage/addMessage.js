// import utilities from './utilities';

const buildMessageCard = () => {
  let domString = '<div>';
  domString += '<p>This is the message</p>';
  domString += '</div>';
  return domString;
  // printToDom('id', domString);
};

export default { buildMessageCard };
